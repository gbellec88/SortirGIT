{% extends 'base.html.twig' %}


{% block body %}
    <h1>Créer un cours</h1>
    <div>
        {{ form_start(sortieForm)}}
        {{ form_widget(sortieForm)}}
        <button class="btn btn-primary btn-sm">Ajouter une Sortie</button>
        {{ form_end(sortieForm)}}
    </div>
{% endblock %}


{% block title %}
    Créer une sortie | {{ parent() }}
{% endblock %}

